enum ForumsStatus { initial, loading, success, error }
enum ProfileStatus { initial, loading, success, error }
enum CommentsStatus { initial, loading, success, error }
enum SaveStatus { initial, loading, success, error }
